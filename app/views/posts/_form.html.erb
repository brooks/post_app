<h1>New Post</h1>

<%= form_for @post do |f| %>

  <label><%= f.label :title %></label>
  <%= f.text_field :title %>
  <label><%= f.label :content %></label>
  <%= f.text_area :content %>

  <%= f.submit %>
  
<% end %>